<Window
    x:Class="FastNote.App.UnsavedChangesDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Notepad"
    Width="456"
    Height="252"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="{DynamicResource WindowBackgroundBrush}"
    Foreground="{DynamicResource MenuForegroundBrush}"
    FontFamily="{DynamicResource EditorFont}"
    Icon="pack://application:,,,/Assets/txtfile.ico">
    <Border Margin="12" Background="{DynamicResource PopupBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Effect="{StaticResource PopupShadow}">
        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Style="{StaticResource DialogTitleTextStyle}" Text="Do you want to save this file first?" />
            <TextBlock x:Name="MessageText" Grid.Row="1" Margin="0,12,0,0" Style="{StaticResource DialogBodyTextStyle}" TextWrapping="Wrap" />
            <Border Grid.Row="2" Margin="0,18,0,0" Padding="14,12" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10">
                <TextBlock x:Name="FileNameText" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
            </Border>
            <StackPanel Grid.Row="3" Margin="0,22,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Width="96" Height="32" Margin="0,0,8,0" IsDefault="True" Style="{StaticResource DialogPrimaryButtonStyle}" Content="Save" Click="SaveButton_OnClick" />
                <Button Width="104" Height="32" Margin="0,0,8,0" Style="{StaticResource DialogButtonStyle}" Content="Don't save" Click="DontSaveButton_OnClick" />
                <Button Width="88" Height="32" IsCancel="True" Style="{StaticResource DialogButtonStyle}" Content="Cancel" Click="CancelButton_OnClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
