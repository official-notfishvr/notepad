<Window
    x:Class="FastNote.App.GoToLineDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Go To Line"
    Width="320"
    Height="160"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    Background="{DynamicResource PopupBackgroundBrush}"
    Icon="pack://application:,,,/Assets/txtfile.ico">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Margin="0,0,0,6" FontSize="12" Foreground="{DynamicResource MenuForegroundBrush}" Text="Line number:" />
        <TextBox x:Name="LineBox" Grid.Row="1" Height="30" Margin="0,0,0,12" FontSize="13" />
        <StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="72" Height="28" Margin="0,0,8,0" IsDefault="True" Content="Go" Click="GoButton_OnClick" />
            <Button Width="72" Height="28" IsCancel="True" Content="Cancel" />
        </StackPanel>
    </Grid>
</Window>
